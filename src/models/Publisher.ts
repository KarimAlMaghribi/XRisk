export interface Publisher {
    name: string;
    address?: string;
    phoneNumber?: string;
    email?: string;
    description?: string;
    age?: number;
    imagePath?: string;
    uid: string;
}
