export interface Publisher {
    uid: string;
    name: string;
    address?: string;
    phoneNumber?: string;
    email?: string;
    description?: string;
    age?: number;
    imagePath?: string;
}
